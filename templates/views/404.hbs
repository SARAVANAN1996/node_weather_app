<!DOCTYPE html>
<html>
    <head>
        <title>{{page}}</title>
        <link rel="stylesheet" href="/css/styles.css">
        <script src="/js/app.js"></script>
    </head>
    <body>
        {{>header}}
        <h2>{{title}}</h2>
        {{>footer}}
    </body>
</html>